# MediaPipe models

Not included in the repository (multi-MB binaries under Google's own model license).

Download `pose_landmarker_lite.task` from the [MediaPipe Tasks Vision model list](https://ai.google.dev/edge/mediapipe/solutions/vision/pose_landmarker)
and place it in this directory before deploying — `pose-game-bridge.js` loads it from
`/models/pose_landmarker_lite.task`.
